<!--
Thanks for the contribution! A short PR description goes a long way.
Delete the sections that don't apply.
-->

## What does this PR do?

<!-- One paragraph. What changes, why, and what does the end-user notice? -->

## Linked issue

<!-- e.g. Closes #42 — link an issue when there is one. -->

## How was this tested?

<!-- Commands you ran locally, or "n/a — docs only".
     Example:
       pnpm typecheck
       pnpm lint
       pnpm test
       pnpm build
-->

## Checklist

- [ ] `pnpm typecheck` passes locally
- [ ] `pnpm lint` passes locally
- [ ] `pnpm test` passes locally
- [ ] Added/updated tests where it makes sense
- [ ] Updated docs (`README.md`, code comments) if behaviour changed
- [ ] My commits follow [Conventional Commits](https://www.conventionalcommits.org/)
      (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`)

## Notes for the reviewer

<!-- Anything reviewer-specific: tradeoffs, follow-ups, things you tried
     and discarded, screenshots, etc. -->
